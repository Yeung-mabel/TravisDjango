language: python
python:
  - “3.9“

# command to install dependencies
install:
  - pip install -r requirements.txt

sudo: false

branches:
  only:
    - master

script:
  - python manage.py runserver
